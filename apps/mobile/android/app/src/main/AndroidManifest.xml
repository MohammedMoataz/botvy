<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required: the app talks to the gateway over HTTP/SSE. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Required for any notification on Android 13+, and requested at runtime
         after sign-in. Declaring it alone shows nothing. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- A reminder is an alarm: it has to fire at the minute the user asked
         for, not whenever the system next batches work. USE_EXACT_ALARM is the
         reminder-app grant (auto-granted on 13+); SCHEDULE_EXACT_ALARM covers
         Android 12, where the user can revoke it and we fall back to inexact. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Pending alarms are dropped by the OS on reboot; the receiver below
         restores them. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- ponytail: cleartext is on so the default http://10.0.2.2:8080 emulator
         target works. Once the gateway is behind the Cloudflare tunnel (https),
         drop this attribute or replace it with a network_security_config that
         allowlists only 10.0.2.2. -->
    <application
        android:label="Botvy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Server pushes land on the same channel as the device's own alarms,
             so the user has one notification switch rather than two. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="botvy_reminders" />

        <!-- flutter_local_notifications: delivers scheduled alarms, and
             re-registers the pending ones after a reboot or an app update. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
