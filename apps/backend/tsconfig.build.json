{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Only the build cares where the output tree is rooted.
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "test", "scripts", "**/*.spec.ts"]
}
