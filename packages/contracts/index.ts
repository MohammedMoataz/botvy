/**
 * @botvy/contracts — the generated view of the API's own contracts.
 *
 * `src/openapi.ts` and `src/graphql.ts` are written by `pnpm gen:contracts`
 * (backend generation mode, task T031, then `scripts/generate.mjs`). They are
 * not committed, so this entry point has to compile before they exist: the two
 * re-exports below are commented out and uncommenting them is the whole of the
 * wiring once generation has run for the first time.
 */

// export type * from './src/openapi.js';
// export type * from './src/graphql.js';

/** True once the generated modules above are re-exported. */
export const CONTRACTS_GENERATED = false;
