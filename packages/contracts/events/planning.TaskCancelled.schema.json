{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "planning.TaskCancelled",
  "description": "Payload of planning.TaskCancelled. It travels inside the envelope in envelope.schema.json.",
  "type": "object",
  "properties": {
    "taskId": {
      "type": "string",
      "format": "uuid"
    },
    "at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "taskId",
    "at"
  ],
  "additionalProperties": false
}
