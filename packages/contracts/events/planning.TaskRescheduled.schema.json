{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "planning.TaskRescheduled",
  "description": "Payload of planning.TaskRescheduled. It travels inside the envelope in envelope.schema.json.",
  "type": "object",
  "properties": {
    "taskId": {
      "type": "string",
      "format": "uuid"
    },
    "dueAt": {
      "anyOf": [
        {
          "type": "string",
          "format": "date-time"
        },
        {
          "type": "null"
        }
      ]
    },
    "allDay": {
      "type": "boolean"
    },
    "title": {
      "type": "string"
    }
  },
  "required": [
    "taskId",
    "dueAt",
    "allDay",
    "title"
  ],
  "additionalProperties": false
}
