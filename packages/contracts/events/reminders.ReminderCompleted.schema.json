{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "reminders.ReminderCompleted",
  "description": "Payload of reminders.ReminderCompleted. It travels inside the envelope in envelope.schema.json.",
  "type": "object",
  "properties": {
    "reminderId": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "reminderId"
  ],
  "additionalProperties": false
}
