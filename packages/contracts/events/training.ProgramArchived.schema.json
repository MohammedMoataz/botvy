{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "training.ProgramArchived",
  "description": "Payload of training.ProgramArchived. It travels inside the envelope in envelope.schema.json.",
  "type": "object",
  "properties": {
    "programId": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "programId"
  ],
  "additionalProperties": false
}
