node_modules/
dist/
build/
.next/
.output/
.wxt/
coverage/

# Generated: the generator owns the shape.
pnpm-lock.yaml
packages/contracts/src/
packages/tokens/dist/
**/*.g.dart

# Prettier has no useful opinion about these.
*.md.tpl

# Generated by the backend's own `gen:contracts`, and the proof artefact two
# rules in CLAUDE.md are checked against. Prettier reformatting it would make
# every regeneration a diff.
packages/contracts/schema.graphql
packages/contracts/openapi.json

# Markdown is out, deliberately, and this is a decision rather than an oversight
# — see docs/decisions/001-the-options-i-chose-on-the-enhancements.md. The
# specs, the enhancement files and CLAUDE.md are hand-wrapped records of
# decisions, most of them historical. Reformatting a document nobody is editing
# adds churn to `git blame` for no review benefit, which is the exact cost
# enforcing Prettier is meant to remove.
*.md

# n8n's own export shape, imported verbatim by `infra/bootstrap.mjs`. Prettier
# would reformat every one and make the next export a whole-file diff.
workflows/*.json
